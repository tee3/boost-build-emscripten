import testing ;

project
  : default-build
    <toolset>emscripten

    # @todo until we have dynamic linking
    <link>static
  ;

lib liba : a.cpp ;

exe hello : hello.cpp ;

run test_pass.cpp ;

run test_a.cpp liba ;
