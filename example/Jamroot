import testing ;

project
  : default-build
    <toolset>emscripten

    # @todo until we have dynamic linking
    <link>static
  ;

run hello.cpp ;

compile a.cpp ;
lib liba : a.cpp ;
run test_a.cpp liba ;
